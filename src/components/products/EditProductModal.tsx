// This file is kept for backward compatibility
// It re-exports the EditLineItemModal component from the new file

import { EditLineItemModal } from '../modals/EditLineItemModal';

// Re-export the component with the old name for backward compatibility
export { EditLineItemModal as EditProductModal };

// Also export the new name
export { EditLineItemModal };
